export interface RegisterData {
  firstName: string;
  lastName: string;
  // companyName: string;
  // taxRegNum: string;
  email: string;
  // country: string;
  // photoUrl?: string;
  // phone: string;
  password: string;
  confirmPassword: string;
}
